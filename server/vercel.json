{
  "version": 2,
  "builds": [
    { "src": "./server.js", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "/api" },
    { "src": "/api/:character", "dest": "/api/:character" },
    { "src": "/(.*)", "dest": "/api" }
  ]
}

